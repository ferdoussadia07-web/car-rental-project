<?php
/**
 * config/db_config.php
 *
 * PLACEHOLDER ONLY — this is normally Task 1's shared file (Adnan's
 * repo already has the real one with the team's actual DB credentials).
 * Do not commit this placeholder over the real one. This file exists
 * here only so admin/model/db.php has something to require() while
 * testing locally before merge.
 */

define("DB_HOST", "localhost");
define("DB_USER", "root");
define("DB_PASS", "");
define("DB_NAME", "car_rental_db");
